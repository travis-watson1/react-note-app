import React from 'react';
import { directive } from '@babel/types';

class Nav extends React.Component {
    render() {
        return(
        <div>
            Nav Component
        </div>
        );
    }
}

export default Nav;